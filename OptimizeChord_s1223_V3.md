# OptimizeChord_s1223_V3 使用说明

S1223 翼型 30 kW 级风轮叶片弦长优化脚本。固定扭角分布，以多风况加权年发电量（AEP）为目标，结合多雷诺数翼型极曲线，在梯度优化框架下求解最优弦长分布。采用多起点策略规避弦长优化中常见的锯齿形局部最优。

---

## 目录

1. [数值模型](#1-数值模型)
2. [优化方法](#2-优化方法)
3. [使用方法](#3-使用方法)
4. [输出说明](#4-输出说明)
5. [代码结构](#5-代码结构)
6. [已知局限](#6-已知局限)

---

## 1. 数值模型

### 1.1 气动求解器：CCBlade（BEM）

核心求解器为 NREL CCBlade，采用叶素动量（BEM）方法。主要特性：

- **收敛保证**：使用 Brent 法（有界二分 + 割线）求解 BEM 非线性方程，不依赖初值，全局收敛
- **连续可微**：通过 Fortran 层的切向导数代码传播解析梯度，输出关于所有输入（包括 `chord`）的一阶偏导数连续
- **修正模型**：
  - Prandtl 叶尖/叶根损失
  - Glauert 大推力区修正（轴向诱导因子 $a > 0.4$ 时）
  - 尾流旋转（切向诱导因子 $a'$）
  - 阻力修正（$C_d$ 计入推力/扭矩）

### 1.2 翼型气动数据：多雷诺数极曲线

**雷诺数范围估算**（V = 5~11 m/s，TSR = 7.98）：

| 展向位置 | r (m) | 弦长 (m) | Re 范围 | 分配翼型 |
|---|---|---|---|---|
| 叶根（圆柱） | 0.30, 0.50 | 0.25 | 0.09~0.25×10⁶ | 圆柱（纯阻力） |
| 过渡段 | 0.68, 0.97 | 0.34~0.48 | 0.27~0.59×10⁶ | **FFA-W3-241** |
| 叶身段 | 1.30~3.50 | 0.09~0.27 | 0.26~0.57×10⁶ | S1223 |

**S1223（叶身段，idx 4~12，t/c = 12.1%）**

| 文件 | Re |
|---|---|
| `S1223_Re0.100.dat` | 1.0 × 10⁵ |
| `S1223_Re0.500.dat` | 5.0 × 10⁵ |
| `S1223_Re0.800.dat` | 8.0 × 10⁵ |
| `S1223_Re1.000.dat` | 1.0 × 10⁶ |
| `S1223_Re2.000.dat` | 2.0 × 10⁶（公共 AOA 基准） |

**FFA-W3-241（过渡段，idx 2~3，t/c = 24.1%）**

| 文件 | Re |
|---|---|
| `FFA-W3-241_Re0.100.dat` | 1.0 × 10⁵ |
| `FFA-W3-241_Re0.500.dat` | 5.0 × 10⁵ |
| `FFA-W3-241_Re0.800.dat` | 8.0 × 10⁵ |
| `FFA-W3-241_Re1.000.dat` | 1.0 × 10⁶ |
| `FFA-W3-241_Re2.000.dat` | 2.0 × 10⁶ |

所有极曲线以 `S1223_Re2.000.dat` 的 AOA 网格为公共基准（n_aoa = 357 点），其余文件线性插值对齐。CCBladeTwist 根据各截面局部 Re 在 5 个档位间自动插值，无需手动分配。

极曲线表维度：`(n_span=13, n_aoa=357, n_Re=5, n_tab=1)`

### 1.3 叶片几何模型

| 参数 | 值 |
|---|---|
| 展向截面数 n_span | 13 |
| 叶根半径 R_hub | 0.20 m |
| 叶尖半径 R_tip | 3.50 m |
| 叶片数 | 2 |
| 叶根截面（圆柱） | idx 0~1：r = 0.30, 0.50 m，弦长固定 0.25 m |
| 过渡截面（FFA-W3-241） | idx 2~3：r = 0.676, 0.967 m，t/c = 24.1% |
| 叶身截面（S1223） | idx 4~12：r = 1.302~3.496 m，t/c = 12.1% |
| 空气密度 | 1.1 kg/m³（海拔 ~1000 m） |
| 动力粘度 | 1.81206×10⁻⁵ kg/(m·s) |

### 1.4 固定扭角输入

弦长优化时，扭角分布作为固定参数输入，不参与优化：

- **默认**：使用 `twist_init_deg`（叶片初始设计扭角）
- **推荐**：加载 `OptimizeTwist_s1223_V3.py` 的优化结果，实现扭角→弦长的接力优化

弦长对气动性能的影响独立于扭角方向的优化，两者接力比同时优化收敛更稳定。

---

## 2. 优化方法

### 2.1 优化目标：多风况加权 AEP

**问题背景**

风场数据（2020–2024 年 7 月 + 9 月，ERA5 925 hPa，海拔 ~1000 m）分析结果：

- 切入风速 3 m/s，切出风速 18 m/s，有效运行时间占比 **45.6%**
- 有效段内 Weibull 分布：k = 2.487，c = 6.156 m/s
- 能量峰值在 7~9 m/s，主力发电区间 6~10 m/s

弦长影响叶片实度（solidity），进而影响各截面推力/扭矩的绝对量。合理的弦长分布应使叶片在**整个运行风速范围**内都有较高的功率提取效率，而非仅在某一设计点最优。

**AEP 目标函数**

$$\text{AEP\_proxy} = \sum_{i=1}^{5} f_i \cdot P(V_i,\ c)$$

- $f_i$：各风速区间的**时间频率权重**（有效段内归一化，$\sum f_i = 1$）
- $P(V_i, c)$：在风速 $V_i$ 和弦长分布 $c$ 下的风轮功率输出（单位 W）
- 扭角 $\theta$ 固定，能量贡献通过 $P \propto V^3$ 自然体现

**代表工况划分**（按等能量贡献选取）：

| 序号 | 代表风速 | 覆盖区间 | 时间频率 $f_i$ | 能量贡献 |
|---|---|---|---|---|
| 1 | 5.0 m/s | [3.0, 5.75) m/s | 53.7% | 18.2% |
| 2 | 6.5 m/s | [5.75, 7.0) m/s | 16.4% | 14.8% |
| 3 | **7.5 m/s** | [7.0, 8.0) m/s | 12.5% | **17.3%**（能量峰值） |
| 4 | 8.5 m/s | [8.0, 9.75) m/s | 8.3% | 16.8% |
| 5 | 11.0 m/s | [9.75, 18.0] m/s | 9.1% | 32.9% |

### 2.2 优化问题数学表述

$$
\begin{aligned}
\min_{c} \quad & -\sum_{i=1}^{5} f_i \cdot P(V_i,\ c) \\
\text{s.t.} \quad & \alpha_j(V_i,\ c) \leq 11° \quad \forall i \in \{1\ldots5\},\ j \in \{1\ldots13\} \\
& 0.05 \leq c_j \leq 0.70\ \text{m} \quad \forall j \in \{2\ldots12\}
\end{aligned}
$$

- **设计变量**：11 个非圆柱截面的弦长 $c_j$（idx 2~12，圆柱截面 idx 0~1 固定为 0.25 m）
- **目标**：最小化 $-\text{AEP\_proxy}$，即最大化加权功率
- **攻角约束**：5 个工况 × 13 个截面 = **65 个不等式约束**，保证各工况均不失速
- **边界约束**：弦长在 [0.05, 0.70] m 范围内（适用于 Rtip=3.5 m 的 30 kW 叶片）

### 2.3 优化算法：SLSQP + 多起点策略

**SLSQP**

| 参数 | 设置 |
|---|---|
| 算法 | SLSQP（SciPy 实现） |
| 最大迭代次数/起点 | 500 |
| 收敛容差 | 1×10⁻⁵ |
| 梯度来源 | CCBlade 解析梯度（含 `∂P/∂chord` 路径）；备选：`approx_totals(fd)` |

**多起点策略**

弦长优化存在"锯齿形局部最优"风险：各截面弦长相互独立，SLSQP 可能收敛到相邻截面大小交替的非物理局部解。为此采用 10 个随机起点：

| 起点 | 生成方式 |
|---|---|
| Run 1 | 原始弦长 `chord`（无扰动） |
| Run 2~10 | 原始弦长 + 比例随机扰动：`noise ~ U(−30%, +50%) × chord[2:]`，裁剪至 [0.05, 0.70] |

收敛质量判断：输出所有起点的 AEP_proxy 标准差，**std < 1 W** 说明各起点收敛到同一解，局部最优风险低。

**每次多起点调用均独立创建新的 `om.Problem`**（`_build_prob()` 函数），避免 OpenMDAO 内部缓存状态污染后续起点。

### 2.4 OpenMDAO 计算图结构

```
chord ──┬──► blade_0 (V=5.0 m/s) ──► P_0, alpha_0, CP_0, ...
        ├──► blade_1 (V=6.5 m/s) ──► P_1, alpha_1, CP_1, ...
        ├──► blade_2 (V=7.5 m/s) ──► P_2, alpha_2, CP_2, ...  ← 能量峰值
        ├──► blade_3 (V=8.5 m/s) ──► P_3, alpha_3, CP_3, ...
        └──► blade_4 (V=11.0m/s) ──► P_4, alpha_4, CP_4, ...

theta_in (固定) ──┤（同时输入所有子系统）

P_0…P_4 ──► AEPAggregator ──► AEP = Σ f_i × P_i

优化器读取 AEP 及其梯度 ∂AEP/∂chord，
同时读取 alpha_0…alpha_4 及其梯度，更新 chord
```

`chord` 作为共享变量被所有 5 个子系统共用；`theta_in` 固定输入，不是设计变量。`AEPAggregator` 的解析偏导 $\partial \text{AEP}/\partial P_i = f_i$ 直接声明为常数。

---

## 3. 使用方法

### 3.1 依赖环境

```bash
pip install openmdao
pip install -e path/to/CCBlade[test]   # 需要 gfortran 编译 Fortran 扩展
```

需要的 Python 包：`numpy`、`scipy`、`openmdao`、`matplotlib`（可选，可视化）

### 3.2 所需文件

运行前确认以下文件与脚本位于同一目录（或修改 `airfoil_dir` 路径）：

```
# S1223 叶身段（idx 4~12）
S1223_Re0.100.dat
S1223_Re0.500.dat
S1223_Re0.800.dat
S1223_Re1.000.dat
S1223_Re2.000.dat        ← 公共 AOA 基准

# FFA-W3-241 过渡段（idx 2~3）
FFA-W3-241_Re0.100.dat
FFA-W3-241_Re0.500.dat
FFA-W3-241_Re0.800.dat
FFA-W3-241_Re1.000.dat
FFA-W3-241_Re2.000.dat

# 圆柱截面（idx 0~1）
Cylinder.dat             ← 可选，缺失时使用 Cl=0, Cd=0.5 合成数据

# 可选：接力扭角优化结果
twist_optimization_results_AEP.npz   ← 由 OptimizeTwist_s1223_V3.py 生成
```

### 3.3 运行

```bash
cd path/to/S1223_30KW_AFFiles
python OptimizeChord_s1223_V3.py
```

脚本顺序执行，无需交互。10 次多起点优化串行运行，总耗时取决于单次迭代速度（analytic 梯度约数分钟；FD 梯度约数十分钟）。

### 3.4 接力扭角优化（推荐工作流）

建议先运行扭角优化，再运行弦长优化：

```
步骤 1: python OptimizeTwist_s1223_V3.py
        → 输出 twist_optimization_results_AEP.npz

步骤 2: 编辑 OptimizeChord_s1223_V3.py，Section 2，取消注释：
        twist_npz = np.load("twist_optimization_results_AEP.npz")
        twist_fixed_deg = twist_npz["twist_opt"]

步骤 3: python OptimizeChord_s1223_V3.py
        → 输出 chord_optimization_results_AEP.npz
```

### 3.5 参数修改指南

**所有用户参数集中在 Section 2（第 73~134 行）**，其余代码无需修改。

#### 3.5.1 转子几何参数

```python
Rhub       = 0.2      # 叶根半径 (m)
Rtip       = 3.5      # 叶尖半径 (m)
hub_height = 10.0     # 轮毂高度 (m)
precone    = 0.0      # 前锥角 (deg)
n_blades   = 2        # 叶片数
```

#### 3.5.2 来流条件

```python
rho      = 1.1        # 空气密度 (kg/m³)
mu       = 1.81206e-5 # 动力粘度 (kg/(m·s))
tsr      = 7.98       # 叶尖速比（部分负荷区最优 TSR）
pitch_deg = 0.0       # 桨距角 (deg)
```

#### 3.5.3 多风况优化参数

```python
V_cases   = np.array([5.0, 6.5, 7.5, 8.5, 11.0])          # 代表风速 (m/s)
f_weights = np.array([0.537, 0.164, 0.125, 0.083, 0.091])  # 时间频率权重
```

要求 `sum(f_weights) = 1.0`，`V_cases` 升序排列，两者长度一致。

#### 3.5.4 叶片展向几何

```python
r              # 展向截面位置 (m)
chord          # 初始弦长 (m)，作为优化起点和扰动基准
twist_init_deg # 初始扭角 (deg)（若不加载 npz，此即固定扭角）
rthick         # 相对厚度 (%)：圆柱=100.0，FFA过渡段=24.1，S1223叶身=12.1
is_cylinder    # 是否圆柱截面（True/False 列表）
```

#### 3.5.5 弦长优化控制

```python
chord_lower = np.full(n_span, 0.05)   # 下界 (m)，可按截面单独设置
chord_upper = np.full(n_span, 0.70)   # 上界 (m)，可按截面单独设置
n_fix = 2                              # 固定截面数（圆柱，不参与优化）
```

修改示例（限制叶尖截面最大弦长）：
```python
chord_upper[10:] = 0.15   # 叶尖 idx 10~12 不超过 0.15 m
```

#### 3.5.6 多起点参数

```python
n_runs = 10          # 起点数量，可增大以提高全局搜索能力
np.random.seed(42)   # 固定随机种子，保证可复现；去掉此行每次结果不同
```

在 `_build_prob()` 函数（第 440 行）内可调整每次优化的迭代上限：
```python
prob.driver.options["maxiter"] = 500   # 每个起点的最大迭代次数
```

#### 3.5.7 扰动范围（多起点噪声）

在多起点循环（第 519~524 行）调整噪声幅度：
```python
noise = np.random.uniform(-0.30, 0.50, size=...) * chord[n_fix:]
# -0.30 = 下扰动幅度（−30%），0.50 = 上扰动幅度（+50%）
```

#### 3.5.8 关注风况

```python
V_eval = [3.0, 5.0, 15.0, 18.0]   # 可改为任意感兴趣的风速列表 (m/s)
```

#### 3.5.9 梯度模式切换

若遇到"Positive directional derivative"警告或梯度不收敛，在 `_build_prob()` 函数（第 484 行）取消注释：
```python
prob.model.approx_totals(method="fd", step=1e-5, form="central")
```
切换为有限差分梯度（每次迭代更慢，但对病态问题更稳健）。

#### 3.5.10 翼型文件路径

```python
airfoil_dir = ""   # 默认：脚本所在目录
# 如翼型文件在其他位置，修改为绝对路径
```

---

## 4. 输出说明

### 4.1 终端打印

**第一阶段：导数连通性验证**
```
第一阶段: 导数连通性验证  (V=7.5 m/s, 能量峰值工况)
--- 基准输出 ---
  CP = ...  P = ...  alpha = [...]
--- 手动扰动检查 (第6截面弦长 +0.05m) ---
  CP (原始)       = x.xxxxxx
  CP (弦长+0.05m) = x.xxxxxx
  ΔCP             = x.xxxxxx
  ✓ CP 对 chord 有响应，导数链连通
```

**第二阶段：多起点优化过程**
```
第二阶段: 多风况加权 AEP 弦长优化（多起点）
代表风速 (m/s)  : [5.0  6.5  7.5  8.5 11.0]
...
开始多起点优化  (n_runs=10) ...
  Run  1/10 ✓: AEP_proxy = xxxxx.x W  (xx.xs)
  Run  2/10 ✓: AEP_proxy = xxxxx.x W  (xx.xs)
  ...
成功收敛: x/10
AEP_proxy 分布: min=xxxxx.x  max=xxxxx.x  std=x.xxe-xx
```

**优化结果汇总**
```
AEP_proxy = xxxxx.x W

工况  V(m/s)  权重f_i    CP      P(W)    f_i×P(W)  能量贡献%
V1    5.0     0.537   0.xxxx  xxxxx    xxxxx      xx.x%
...
推力 ≈ xxxxx N  (V=7.5 m/s 工况)
截面气动量表（弦长对比、攻角、Cl/Cd）
```

**关注风况对比**（同 OptimizeTwist_s1223_V3 格式，包含 3/5/15/18 m/s 四个风速）

### 4.2 保存文件

| 文件 | 内容 |
|---|---|
| `chord_optimization_results_AEP.npz` | 优化结果数值数据（见下表） |
| `chord_optimization_AEP.png` | 2×3 可视化图（dpi=150） |

**npz 文件字段**：

| 键 | 形状 | 说明 |
|---|---|---|
| `r` | (13,) | 展向位置 (m) |
| `chord_init` | (13,) | 初始弦长 (m) |
| `chord_opt` | (13,) | 优化弦长 (m) |
| `twist_fixed` | (13,) | 本次使用的固定扭角 (deg) |
| `V_cases` | (5,) | 代表风速 (m/s) |
| `f_weights` | (5,) | 时间频率权重 |
| `P_cases` | (5,) | 各工况优化后功率 (W) |
| `CP_cases` | (5,) | 各工况优化后 CP |
| `AEP` | scalar | 加权平均功率 (W) |
| `T_approx` | scalar | 能量峰值工况推力近似值 (N) |
| `alpha_peak` | (13,) | 能量峰值工况攻角 (deg) |
| `cl_peak` | (13,) | 能量峰值工况升力系数 |
| `cd_peak` | (13,) | 能量峰值工况阻力系数 |

读取示例：
```python
import numpy as np
data = np.load("chord_optimization_results_AEP.npz")
chord_opt = data["chord_opt"]   # 最优弦长分布
AEP       = data["AEP"]         # 加权平均功率
```

### 4.3 可视化图（2×3 布局）

| 位置 | 内容 |
|---|---|
| [0,0] | 弦长分布（初始 vs 优化） |
| [0,1] | 各工况攻角沿展向分布 + 11° 失速限制线 |
| [0,2] | 各工况功率柱状图 + AEP 贡献折线（双轴） |
| [1,0] | 多起点 AEP_proxy 收敛散点图（绿=收敛，红=未收敛） |
| [1,1] | 升阻比 Cl/Cd 展向分布（能量峰值工况） |
| [1,2] | AEP 贡献饼图（各工况 $f_i \times P_i$ 占比） |

---

## 5. 代码结构

| Section | 行号 | 功能 |
|---|---|---|
| 1 | 37–71 | 翼型文件解析函数（`load_aerodyn_polar`、`make_cylinder_polar`） |
| **2** | **73–134** | **★ 用户参数区（转子几何、来流、多风况参数、叶片展向数据、弦长边界、固定扭角）** |
| 3 | 137–239 | 多翼型多 Re 极曲线加载与表格构建（S1223 叶身 + FFA-W3-241 过渡段 + 圆柱） |
| 4 | 242–283 | OpenMDAO `modeling_options` / `opt_options` 配置 |
| 5 | 286–315 | `AEPAggregator` 组件（解析梯度，$\partial\text{AEP}/\partial P_i = f_i$） |
| 6 | 317–375 | 共享输入列表 `_SHARED_INPUTS` 和 `set_shared_inputs()` 函数（含固定 `theta_in`） |
| 7 | 377–423 | 导数连通性验证（单工况 BEM，弦长扰动检查） |
| 8 | 425–543 | 多风况 AEP 弦长优化主体（`_build_prob()`、`_run_one()`、多起点循环），多起点汇总 |
| **8c** | **~585** | **弦长单调性后处理**：`_monotone_smooth_chord()` + `_eval_aep_only()`；用 UnivariateSpline（最小光滑因子）→ PAV 回退确保 idx n_fix: 弦长单调递减，并报告 AEP 损失 |
| 9 | 600–655 | 优化结果打印与 `.npz` 保存 |
| 10 | 658–734 | 指定风况（3/5/15/18 m/s）优化前后对比评估 |
| 11 | 737–815 | 可视化（2×3 子图，含多起点收敛散点图） |

---

## 6. 已知局限

| 局限 | 说明 | 建议改进方向 |
|---|---|---|
| **锯齿形局部最优** | 各截面弦长独立优化，SLSQP 可能收敛到相邻截面大小交替的非物理解；当前通过样条光滑+PAV 后处理修复 | 在优化问题中直接添加相邻截面弦长差约束 `c_j ≥ c_{j+1}` 或单调B样条参数化，使优化器原生保证单调性 |
| **圆柱段弦长未论证** | idx 0~1 弦长固定 0.250 m，r=0.50→0.676 m 弦长从 0.250 跳到 0.506（变化率 1.45 m/m），过渡突兀 | 通过根部弯矩计算确定结构最小直径；将 idx 1（r=0.50 m）弦长适当提高（~0.35 m）平滑过渡 |
| **厚度过渡突变** | rthick 在 idx 3→4（r=0.967→1.302 m）从 24.1% 跳至 12.1%，缺少 18~21% 中间过渡翼型 | 增加 FFA-W3-211（21%）截面；或引入 NACA 63-618（18%）作为中间过渡 |
| **单一叶身翼型** | 叶身段（idx 4~12）均用 S1223（t/c=12.1%），内叶身（r=1.3~1.7 m）偏薄不利于结构 | 可为 idx 4~5 引入 FFA-W3-211（21%）改善内叶身结构效率 |
| **固定扭角假设** | 弦长优化时扭角保持固定，两者实际上相互耦合 | 弦长与扭角联合优化：同时以 `theta_in` 和 `chord[2:]` 为设计变量 |
| **固定 TSR** | 所有工况假设最优 TSR = 7.98，11 m/s 若超额定应变桨 | 对 11 m/s 工况增加变桨角作为独立设计变量 |
| **推力 T 输出恒为 0** | `CCBladeTwist.compute()` 中 T 未赋值，推力通过 `Px_b` 积分近似 | 若需推力约束，使用 `Px_b` 积分替代，或从源码修复 |
| **无结构约束** | 未考虑叶片弯矩、挠度、疲劳载荷 | 结合 OpenFAST 或 BeamDyn 添加结构响应约束 |
| **多起点串行** | 10 次优化顺序执行，不并行 | 用 `multiprocessing` 或 `joblib` 并行多起点，缩短总耗时 |
