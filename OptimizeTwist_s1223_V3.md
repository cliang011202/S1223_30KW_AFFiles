# OptimizeTwist_s1223_V3 使用说明

S1223 翼型 30 kW 级风轮叶片扭角优化脚本。基于多风况加权年发电量（AEP）目标，结合多雷诺数翼型极曲线，在梯度优化框架下求解最优扭角分布。

---

## 目录

1. [数值模型](#1-数值模型)
2. [优化方法](#2-优化方法)
3. [使用方法](#3-使用方法)
4. [输出说明](#4-输出说明)
5. [代码结构](#5-代码结构)
6. [已知局限](#6-已知局限)

---

## 1. 数值模型

### 1.1 气动求解器：CCBlade（BEM）

核心求解器为 NREL CCBlade，采用叶素动量（BEM）方法。主要特性：

- **收敛保证**：使用 Brent 法（有界二分 + 割线）求解 BEM 非线性方程，不依赖初值，全局收敛
- **连续可微**：通过 Fortran 层的切向导数代码（`_bem.inductionfactors_dv` / `_bem.relativewind_dv`）传播解析梯度，输出关于所有输入的一阶偏导数连续
- **修正模型**：
  - Prandtl 叶尖/叶根损失
  - Glauert 大推力区修正（轴向诱导因子 $a > 0.4$ 时）
  - 尾流旋转（切向诱导因子 $a'$）
  - 阻力修正（$C_d$ 计入推力/扭矩）

BEM 的核心假设：叶片无限展弦比、不可压流、无三维效应。

### 1.2 翼型气动数据：多雷诺数极曲线

**雷诺数范围估算**（V = 5~11 m/s，TSR = 7.98）：

| 展向位置 | r (m) | 弦长 (m) | Re 范围 | 分配翼型 |
|---|---|---|---|---|
| 叶根（圆柱） | 0.30, 0.50 | 0.25 | 0.09~0.25×10⁶ | 圆柱（纯阻力） |
| 过渡段 | 0.68, 0.97 | 0.34~0.48 | 0.27~0.59×10⁶ | **FFA-W3-241** |
| 叶身段 | 1.30~3.50 | 0.09~0.27 | 0.26~0.57×10⁶ | S1223 |

原代码固定使用 Re = 2×10⁶ 且全叶身采用 S1223，存在两个问题：**Re 高估 3~20 倍**导致翼型性能被高估；**圆柱紧接 12.1% 薄翼型**的几何突变在结构上不合理。

本版本对每个截面按实际情况分配翼型和 Re 档位：

**S1223（叶身段，idx 4~12，t/c = 12.1%）**

| 文件 | Re |
|---|---|
| `S1223_Re0.100.dat` | 1.0 × 10⁵ |
| `S1223_Re0.500.dat` | 5.0 × 10⁵ |
| `S1223_Re0.800.dat` | 8.0 × 10⁵ |
| `S1223_Re1.000.dat` | 1.0 × 10⁶ |
| `S1223_Re2.000.dat` | 2.0 × 10⁶ |

**FFA-W3-241（过渡段，idx 2~3，t/c = 24.1%）**

| 文件 | Re |
|---|---|
| `FFA-W3-241_Re0.100.dat` | 1.0 × 10⁵ |
| `FFA-W3-241_Re0.500.dat` | 5.0 × 10⁵ |
| `FFA-W3-241_Re0.800.dat` | 8.0 × 10⁵ |
| `FFA-W3-241_Re1.000.dat` | 1.0 × 10⁶ |
| `FFA-W3-241_Re2.000.dat` | 2.0 × 10⁶ |

FFA-W3-241 为风力机专用厚翼型，在过渡段实际工作 Re（0.27~0.59×10⁶）下 $(L/D)_\text{max}$ 约 81~105，失速角约 14~15°，失速特性温和；相对厚度 24.1% 为圆柱（100%）到叶身（12.1%）提供合理的几何过渡，符合工业设计标准。

所有极曲线以 `S1223_Re2.000.dat` 的 AOA 网格为公共基准，其余文件通过线性插值对齐。CCBladeTwist 在求解时根据各截面局部速度和弦长计算实际 Re，在 5 个档位之间自动插值，无需手动分配。

极曲线表维度：`(n_span=13, n_aoa=357, n_Re=5, n_tab=1)`

### 1.3 叶片几何模型

| 参数 | 值 |
|---|---|
| 展向截面数 n_span | 13 |
| 叶根半径 R_hub | 0.20 m |
| 叶尖半径 R_tip | 3.50 m |
| 叶片数 | 2 |
| 叶根截面（圆柱） | idx 0~1：r = 0.30, 0.50 m |
| 过渡截面（FFA-W3-241） | idx 2~3：r = 0.676, 0.967 m，t/c = 24.1% |
| 叶身截面（S1223） | idx 4~12：r = 1.302~3.496 m，t/c = 12.1% |
| 空气密度 | 1.1 kg/m³（海拔 ~1000 m） |
| 动力粘度 | 1.81206×10⁻⁵ kg/(m·s) |

---

## 2. 优化方法

### 2.1 优化目标：多风况加权 AEP

**问题背景**

风场数据（2020–2024 年 7 月 + 9 月，ERA5 925 hPa，海拔 ~1000 m）分析结果：

- 切入风速 3 m/s，切出风速 18 m/s，有效运行时间占比 **45.6%**
- 有效段内 Weibull 分布：k = 2.487，c = 6.156 m/s
- 能量峰值在 7~9 m/s，主力发电区间 6~10 m/s

原设计风速 V = 18 m/s（切出风速）在该风场出现概率 < 0.1%，以此优化的叶片在实际运行中贡献极少。

**AEP 目标函数**

$$\text{AEP\_proxy} = \sum_{i=1}^{5} f_i \cdot P(V_i,\ \theta)$$

- $f_i$：各风速区间的**时间频率权重**（有效段内归一化，$\sum f_i = 1$）
- $P(V_i, \theta)$：在风速 $V_i$ 和扭角分布 $\theta$ 下的风轮功率输出（单位 W）
- 能量贡献通过 $P \propto V^3$ 自然体现，$f_i$ 无需额外乘 $V^3$

> **关键**：优化变量 $\theta$ 对所有 5 个工况是同一套扭角分布，优化器寻找在所有风速的加权意义下最优的扭角。

**代表工况划分**（按等能量贡献选取）：

| 序号 | 代表风速 | 覆盖区间 | 时间频率 $f_i$ | 能量贡献 |
|---|---|---|---|---|
| 1 | 5.0 m/s | [3.0, 5.75) m/s | 53.7% | 18.2% |
| 2 | 6.5 m/s | [5.75, 7.0) m/s | 16.4% | 14.8% |
| 3 | **7.5 m/s** | [7.0, 8.0) m/s | 12.5% | **17.3%**（能量峰值） |
| 4 | 8.5 m/s | [8.0, 9.75) m/s | 8.3% | 16.8% |
| 5 | 11.0 m/s | [9.75, 18.0] m/s | 9.1% | 32.9% |

### 2.2 优化问题数学表述

$$
\begin{aligned}
\min_{\theta} \quad & -\sum_{i=1}^{5} f_i \cdot P(V_i,\ \theta) \\
\text{s.t.} \quad & \alpha_j(V_i,\ \theta) \leq \alpha_j^{\max} \quad \forall i \in \{1\ldots5\},\ j \in \{2\ldots12\}
  \quad (\alpha^{\max} = 9°\ \text{FFA},\ 10°\ \text{S1223}) \\
& \theta_j \geq \theta_{j+1} \quad \forall j \in \{2\ldots11\} \quad \text{（单调递减约束）} \\
& \theta_j^{\text{init}} - 8° \leq \theta_j \leq \theta_j^{\text{init}} + 8° \quad \forall j \in \{2\ldots12\}
\end{aligned}
$$

- **设计变量**：11 个非圆柱截面的扭角 $\theta_j$（idx 2~12，弧度）
- **目标**：最小化 $-\text{AEP\_proxy}$，即最大化加权功率
- **攻角约束**：5 个工况 × 11 个截面 = **55 个不等式约束**，分截面限制，保证各工况均不失速
- **单调递减约束**：10 个约束，确保扭角从根部到叶尖严格递减；对等 TSR 变速风机，入流角 $\phi(r)$ 随 $r$ 单调减小，此约束符合物理最优，且从源头消除翼型过渡段的扭角"平台"问题
- **边界约束**：每个截面扭角在初始值 ±8° 范围内搜索

### 2.3 优化算法：SLSQP

采用 Sequential Least Squares Quadratic Programming（序列最小二乘二次规划）：

| 参数 | 设置 |
|---|---|
| 算法 | SLSQP（SciPy 实现） |
| 最大迭代次数 | 1500 |
| 收敛容差 | 1×10⁻⁵ |
| 梯度来源 | CCBlade 解析梯度（通过 OpenMDAO 自动微分链组装） |

SLSQP 是梯度型方法，依赖准确的梯度信息。CCBlade 提供解析梯度（通过 Fortran 切向导数代码），比有限差分快 1~2 个数量级，且梯度精度高，是 SLSQP 在此问题上高效收敛的前提。

### 2.4 OpenMDAO 计算图结构

```
theta_in ──┬──► blade_0 (V=5.0 m/s) ──► P_0, alpha_0, CP_0, ...
           ├──► blade_1 (V=6.5 m/s) ──► P_1, alpha_1, CP_1, ...
           ├──► blade_2 (V=7.5 m/s) ──► P_2, alpha_2, CP_2, ...  ← 能量峰值
           ├──► blade_3 (V=8.5 m/s) ──► P_3, alpha_3, CP_3, ...
           └──► blade_4 (V=11.0m/s) ──► P_4, alpha_4, CP_4, ...

P_0…P_4 ──► AEPAggregator ──► AEP = Σ f_i × P_i

优化器读取 AEP 及其梯度 ∂AEP/∂theta_in，
同时读取 alpha_0…alpha_4 及其梯度，更新 theta_in
```

`theta_in` 作为共享变量被所有 5 个子系统共用，5 路 BEM 并行求解（OpenMDAO 单线程顺序执行），梯度由各子系统的 `compute_partials` 自动汇聚。

`AEPAggregator` 的解析偏导 $\partial \text{AEP}/\partial P_i = f_i$ 直接声明为常数，无运行时开销。

---

## 3. 使用方法

### 3.1 依赖环境

```bash
pip install openmdao
pip install -e path/to/CCBlade[test]   # 需要 gfortran 编译 Fortran 扩展
```

需要的 Python 包：`numpy`、`scipy`、`openmdao`、`matplotlib`（可选，可视化）

### 3.2 所需文件

运行前确认以下文件与脚本位于同一目录（或修改 `airfoil_dir` 路径）：

```
# S1223 叶身段（idx 4~12）
S1223_Re0.100.dat        ← Re = 1.0×10⁵ 极曲线（公共 AOA 基准由 Re2.000 提供）
S1223_Re0.500.dat        ← Re = 5.0×10⁵ 极曲线
S1223_Re0.800.dat        ← Re = 8.0×10⁵ 极曲线
S1223_Re1.000.dat        ← Re = 1.0×10⁶ 极曲线
S1223_Re2.000.dat        ← Re = 2.0×10⁶ 极曲线（公共 AOA 基准）

# FFA-W3-241 过渡段（idx 2~3）
FFA-W3-241_Re0.100.dat   ← Re = 1.0×10⁵ 极曲线
FFA-W3-241_Re0.500.dat   ← Re = 5.0×10⁵ 极曲线
FFA-W3-241_Re0.800.dat   ← Re = 8.0×10⁵ 极曲线
FFA-W3-241_Re1.000.dat   ← Re = 1.0×10⁶ 极曲线
FFA-W3-241_Re2.000.dat   ← Re = 2.0×10⁶ 极曲线

# 圆柱截面（idx 0~1）
Cylinder.dat             ← 圆柱极曲线（可选，缺失时使用 Cl=0, Cd=0.5 合成数据）
```

翼型文件格式：AeroDyn / QBlade 格式，数据行为 `AOA  Cl  Cd  Cm`，非数字行自动跳过。

### 3.3 运行

```bash
cd path/to/S1223_30KW_AFFiles
python OptimizeTwist_s1223_V3.py
```

脚本顺序执行，无需交互，自动完成验证、优化、输出、可视化全流程。

### 3.4 参数修改指南

**所有用户参数集中在 Section 2（第 69~122 行）**，其余代码无需修改。

#### 3.4.1 转子几何参数

```python
Rhub       = 0.2      # 叶根半径 (m)
Rtip       = 3.5      # 叶尖半径 (m)
hub_height = 10.0     # 轮毂高度 (m)
precone    = 0.0      # 前锥角 (deg)
tilt       = 0.0      # 仰角 (deg)
yaw        = 0.0      # 偏航误差 (deg)
n_blades   = 2        # 叶片数
```

#### 3.4.2 来流条件

```python
rho      = 1.1        # 空气密度 (kg/m³)，海拔 ~1000m 取 1.1
mu       = 1.81206e-5 # 动力粘度 (kg/(m·s))
shearExp = 0.0        # 风切指数（轮毂高度 10m AGL，修正微小，取 0）
tsr      = 7.98       # 设计叶尖速比（变速风机在部分负荷区保持此值）
pitch_deg = 0.0       # 桨距角 (deg)，部分负荷区取 0
```

#### 3.4.3 多风况优化参数

```python
V_cases   = np.array([5.0, 6.5, 7.5, 8.5, 11.0])    # 代表风速 (m/s)
f_weights = np.array([0.537, 0.164, 0.125, 0.083, 0.091])  # 时间频率权重
```

**如何更新风况参数**：根据 `wind_data_analysis.md` 中的 Weibull 分析结果修改。要求 `sum(f_weights) = 1.0`，`V_cases` 升序排列，两者长度一致。

#### 3.4.4 叶片展向几何

```python
r              # 展向截面位置 (m)，n_span 个值
chord          # 各截面弦长 (m)
twist_init_deg # 初始扭角 (deg)，同时作为优化起点和边界中心
rthick         # 相对厚度 (%)：圆柱=100.0，FFA过渡段=24.1，S1223叶身=12.1
is_cylinder    # 是否为圆柱截面（True/False 列表）
```

如需更改过渡段翼型，同时修改 Section 3 中的 `ffa_re_files` 和 `is_ffa`，以及此处的 `rthick` 对应位置。

#### 3.4.5 优化控制

```python
# 扭角变动范围（以初始值为中心）
lower = twist_init_deg - 8.0  # 下界 (deg)
upper = twist_init_deg + 8.0  # 上界 (deg)

# 失速约束阈值
prob2.model.add_constraint(f"alpha_{i}", upper=11.0, units="deg")
# 修改 11.0 以调整失速裕量

# 优化器迭代参数
prob2.driver.options["maxiter"] = 1500
prob2.driver.options["tol"]     = 1e-5
```

#### 3.4.6 关注风况评估

```python
V_eval = [3.0, 5.0, 15.0, 18.0]   # 可改为任意感兴趣的风速列表 (m/s)
```

#### 3.4.7 翼型文件路径

```python
airfoil_dir = ""   # 默认：脚本所在目录
# 如翼型文件在其他位置，修改为绝对路径：
# airfoil_dir = "C:/path/to/airfoils"
```

---

## 4. 输出说明

### 4.1 终端打印

脚本按顺序打印以下内容：

**第一阶段：导数连通性验证**
```
第一阶段: 导数连通性验证  (V=7.5 m/s, 能量峰值工况)
--- 基准输出 ---
CP = ...  P = ...  alpha = [...]
--- 手动扰动检查 ---
ΔCP = ...  ✓ CP 对 theta_in 有响应
```

**第二阶段：优化过程**（SLSQP 迭代信息）
```
开始优化...
  NIT   FC   OBJFUN  GNORM
  ...
总耗时: xx.x 秒
✓ 优化收敛成功!
```

**优化结果汇总**
```
AEP_proxy = xxxxx W
工况  V(m/s)  权重f_i    CP      P(W)    f_i×P(W)  能量贡献%
V1    5.0     0.537   0.xxxx  xxxxx    xxxxx      xx.x%
...
推力 ≈ xxxxx N  (V=7.5 m/s 工况)
截面气动量表（扭角对比、攻角、Cl/Cd）
```

**关注风况对比**
```
关注风况：优化前后对比  (V = 3 / 5 / 15 / 18 m/s)
                       V=3 m/s          V=15 m/s         V=18 m/s
                  CP / P(W) / T(N)  CP / P(W) / T(N)  CP / P(W) / T(N)
优化前 (初始扭角)  ...
优化后 (AEP最优)   ...
各项变化量:
   ΔCP   +x.xxxx  +x.xxxx  +x.xxxx
  ΔP(W)  +xxxxx   +xxxxx   +xxxxx
  ΔT(N)  +xxxxx   +xxxxx   +xxxxx
```

### 4.2 保存文件

| 文件 | 内容 |
|---|---|
| `twist_optimization_results_AEP.npz` | 优化结果数值数据（见下表） |
| `twist_optimization_AEP.png` | 2×3 可视化图（dpi=150） |

**npz 文件字段**：

| 键 | 形状 | 说明 |
|---|---|---|
| `r` | (13,) | 展向位置 (m) |
| `chord` | (13,) | 弦长 (m) |
| `twist_init` | (13,) | 初始扭角 (deg) |
| `twist_opt` | (13,) | 优化扭角 (deg) |
| `V_cases` | (5,) | 代表风速 (m/s) |
| `f_weights` | (5,) | 时间频率权重 |
| `P_cases` | (5,) | 各工况优化后功率 (W) |
| `CP_cases` | (5,) | 各工况优化后 CP |
| `AEP` | scalar | 加权平均功率 (W) |
| `alpha_peak` | (13,) | 能量峰值工况攻角 (deg) |
| `cl_peak` | (13,) | 能量峰值工况升力系数 |
| `cd_peak` | (13,) | 能量峰值工况阻力系数 |
| `a_peak` | (13,) | 能量峰值工况轴向诱导因子 |
| `ap_peak` | (13,) | 能量峰值工况切向诱导因子 |
| `T_approx` | scalar | 能量峰值工况推力近似值 (N) |

读取示例：
```python
import numpy as np
data = np.load("twist_optimization_results_AEP.npz")
twist_opt = data["twist_opt"]   # 最优扭角分布
AEP       = data["AEP"]         # 加权平均功率
```

### 4.3 可视化图（2×3 布局）

| 位置 | 内容 |
|---|---|
| [0,0] | 扭角分布（初始 vs 优化） |
| [0,1] | 各工况攻角沿展向分布 + 11° 失速限制线 |
| [0,2] | 各工况功率柱状图 + AEP 贡献折线（双轴） |
| [1,0] | 升阻比 Cl/Cd 展向分布（能量峰值工况） |
| [1,1] | 轴向诱导因子 a 和切向诱导因子 a' 分布（能量峰值工况） |
| [1,2] | AEP 贡献饼图（各工况 $f_i \times P_i$ 占比） |

---

## 5. 代码结构

| Section | 行号 | 功能 |
|---|---|---|
| 1 | 37–66 | 翼型文件解析函数（`load_aerodyn_polar`、`make_cylinder_polar`） |
| **2** | **69–159** | **★ 用户参数区（转子几何、来流、多风况参数、叶片展向数据）+ BEM Betz 最优初始扭角计算（`_betz_twist_deg()`、`twist_betz_deg`）** |
| 3 | 162–262 | 多翼型多 Re 极曲线加载与表格构建（S1223 叶身 + FFA-W3-241 过渡段 + 圆柱） |
| 4 | 267–309 | OpenMDAO `modeling_options` / `opt_options` 配置 |
| 5 | 312–342 | `AEPAggregator` 组件（解析梯度，$\partial\text{AEP}/\partial P_i = f_i$） |
| 6 | 344–398 | 共享输入列表 `_SHARED_INPUTS` 和 `set_shared_inputs()` 函数 |
| 7 | 400–448 | 导数连通性验证（单工况 BEM，手动扰动检查） |
| **8** | **~460** | **`TwistMonoComp`** 常数 Jacobian 稀疏组件（输出相邻截面扭角差，供单调约束使用） |
| 8 | ~498–625 | 多风况 AEP 优化主体：`_build_prob_twist()`（含单调约束 `twist_mono.twist_mono_diff≥0`）、`_run_one_twist()`、多起点循环（Run 0=BEM最优；Run 1=原始设计；Run 2+=BEM最优+扰动） |
| **8b** | **~622** | **单调性断言验证**（约束已内置于优化器，此处仅做数值检查） |
| 9 | ~640–703 | 优化结果打印与 `.npz` 保存 |
| 10 | ~705–790 | 指定风况（3/5/15/18 m/s）优化前后对比评估 |
| 11 | ~792–865 | 可视化（2×3 子图） |

---

## 6. 已知局限

| 局限 | 说明 | 建议改进方向 |
|---|---|---|
| **局部最优风险** | SLSQP 为局部优化器，对初始值敏感 | 多起点扰动，或先用差分进化（DE）全局预搜索再精化 |
| **单一叶身翼型** | 叶身段（idx 4~12）均用 S1223（t/c=12.1%），未考虑截面厚度渐变；过渡段（idx 2~3）已引入 FFA-W3-241（24.1%）| 可为 idx 4~5（r=1.3~1.7 m）引入 FFA-W3-211（21%）以改善内叶身结构效率 |
| **厚度过渡突变** | rthick 在 idx 3→4（r=0.967→1.302 m）从 24.1% 跳至 12.1%，缺少 18~21% 中间过渡翼型 | 增加 FFA-W3-211 截面；或引入 NACA 63-618 等 18% 厚翼型 |
| **圆柱段弦长未论证** | idx 0~1 弦长固定 0.250 m，未经结构分析验证是否满足根部弯矩要求，也未优化过渡段衔接 | 通过根部弯矩计算确定结构最小直径；同时将 idx 1（r=0.50 m）弦长适当加大（~0.35 m）以平滑到 FFA 最大弦长的过渡 |
| **单调约束初始可行性** | 若多起点扰动后的初始扭角本身不满足单调性，SLSQP 从不可行点出发有时收敛更慢 | 扰动时对 `twist_betz_deg` 逐截面从根到尖做单调修正，确保起点本身单调 |
| **固定 TSR** | 所有风速均假设最优 TSR，11 m/s 工况如超额定功率应变桨 | 对 11 m/s 工况增加变桨角作为独立设计变量 |
| **固定转速/变速假设** | 以恒定 TSR = 7.98 代表变速运行，不考虑转速上限 | 引入额定转速约束，切换为功率控制策略 |
| **推力 T 输出恒为 0** | `CCBladeTwist.compute()` 中 T 未赋值，推力通过 `Px_b` 积分近似 | 若需推力约束，建议从源码修复或使用 `Px_b` 积分替代 |
| **无结构约束** | 未考虑叶片根部弯矩、挠度、疲劳载荷 | 结合 OpenFAST 或 BeamDyn 添加结构响应约束 |
| **风场样本季节偏差** | 数据仅含 7 月和 9 月（无 8 月），且仅为夏秋季，Weibull 参数可能偏离全年 | 补充全年数据或按季节分段加权 |
